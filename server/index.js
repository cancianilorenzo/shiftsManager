"use strict";

const userDao = require("./dao");

// for (let i = 0; i < 100; i++) {
//  userDao.addUser('User '+i, 'user', 'password', 0);
// }

userDao.addUser('000 User', 'user', 'password', 0);

// userDao.generateShifts(9, 2024);
